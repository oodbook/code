package se.kth.ict.oodbook.design.uml;

public class ClassF {
    private int count;
    private ClassG classG;
    private ClassH classH;

    /* The following code appears somewhere, in some method:
     *   if (count == 3) {
     *       classG.aMethod();
     *   } else {
     *       classH.aMethod();
     *   }
     */
}
